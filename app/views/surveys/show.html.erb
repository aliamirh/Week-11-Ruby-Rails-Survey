<h1><%= @survey.name %></h1>

<% if @questions.any? %>
<ol>
  <% @questions.each do |question| %>
  <li><%= link_to question.text, survey_question_path(@survey, question) %></li>
  <% end %>
</ol>
<% else %>
<p>There are no questions yet.</p>
<% end %>

<p><%= link_to "Add questions", new_survey_question_path(@survey) %></p>
<p><%= link_to "Edit", edit_survey_path(@survey) %></p>
<p><%= link_to "Delete", survey_path(@survey),
                         :data => {:confirm => "You sure?",
                                   :method => "delete"} %></p>

<p><%= link_to "Return to surveys", surveys_path %></p>
